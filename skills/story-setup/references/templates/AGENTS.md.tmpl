# {项目名} — 网文写作工具集

> 本项目支持多种 AI 编程工具：Claude Code、Kilo、OpenClaw、TRAE 等

## Skill/Command 路由表

| 命令 | Skill/Command | 说明 |
|------|---------------|------|
| `/story-long-write`、`/写长篇` | story-long-write | 长篇网文写作（逐章推进） |
| `/story-short-write`、`/写短篇` | story-short-write | 短篇网文写作（情绪驱动） |
| `/story-long-analyze`、`/长篇拆文` | story-long-analyze | 长篇小说深度拆解 |
| `/story-short-analyze`、`/短篇拆文` | story-short-analyze | 短篇小说拆文分析 |
| `/story-long-scan`、`/长篇扫描` | story-long-scan | 长篇小说批量扫描 |
| `/story-short-scan`、`/短篇扫描` | story-short-scan | 短篇小说批量扫描 |
| `/story-deslop`、`/去AI味` | story-deslop | 去除 AI 写作痕迹 |
| `/story-cover`、`/封面` | story-cover | 生成封面图 |
| `/story-review`、`/审查` | story-review | 多视角对抗式审查 |
| `/story-import`、`/导入` | story-import | 逆向导入已有小说到项目结构 |
| `/story`、`/网文` | story | 工具箱路由 · 模糊意图自动分发 |
| `/story-setup`、`/准备写书` | story-setup | 环境部署 · agents/配置 一键部署 |
| `/browser-cdp` | browser-cdp | 浏览器 CDP 工具 |

## 文件结构

### 长篇小说
- `{书名}/正文/` — 正文章节
- `{书名}/设定/` — 角色设定、世界设定、势力
- `{书名}/大纲/` — 卷纲、细纲
- `{书名}/追踪/` — 上下文.md、伏笔.md、时间线.md
- `{书名}/对标/` — 对标作品分析
- `{书名}/参考资料/` — 研究资料

### 短篇小说
- `短篇/{标题}/正文.md` — 完成稿
- `短篇/{标题}/小节大纲.md` — 结构大纲

### 分析结果
- `拆文库/{书名}/` — 拆文分析结果

## Agent 体系

项目包含 7 个专业 Agent（按需调用）：

| Agent | 职责 | 模型建议 |
|-------|------|----------|
| story-architect | 故事架构、世界观、大纲设计 | 高质量模型 |
| character-designer | 角色设定、语言风格 | 标准模型 |
| narrative-writer | 正文写作、去AI味 | 标准模型 |
| consistency-checker | 一致性检查 | 轻量模型 |
| story-researcher | 资料研究 | 标准模型 |
| story-explorer | 故事查询、上下文加载 | 轻量模型 |
| chapter-extractor | 章节提取 | 轻量模型 |

### Agent 调用方式

不同工具的 Agent 调用方式略有不同，详见 `docs/agent-compatibility.md`。

## 协作规则

1. Agent 间协调由各 Agent 定义文件的职责边界描述
2. 写作时遵循各 skill 的执行规则
3. 所有正文输出前必须经过一致性检查

## 上下文恢复

如果存在 `{书名}/追踪/上下文.md`，在对话压缩后首先读取恢复上下文。

关键上下文包括：
1. 当前写作项目名称和进度
2. 最近讨论的角色设定变更
3. 未完成的伏笔列表
4. 当前章节的情绪/节奏目标

## 多工具兼容性

- **Claude Code / OpenClaw**：使用 `.claude/` 配置目录
- **Kilo**：使用 `.kilo/` 配置目录和 `kilo.json`
- **其他工具**：使用 `skills/` 目录和 `AGENTS.md`

详细迁移指南见 `docs/migration-guide.md`。
